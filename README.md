# gravl_spec-
GRAVL is a 2D esoteric language. A read/write head moves across a toroidal 16x16 grid of 8-bit cells (all initialized to 0). An accumulator (acc, 8-bit, init 0) handles computation. The head starts at position (0, 0), facing East.
